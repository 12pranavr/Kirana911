{
  "dependencies": {
    "@supabase/supabase-js": "^2.86.0",
    "dotenv": "^17.2.3"
  },
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "build": "cd frontend; npm run build",
    "vercel-build": "cd frontend; npm run build"
  }
}